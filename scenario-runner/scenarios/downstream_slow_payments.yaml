id: downstream_slow_payments
description: "Ralentissement downstream sur payments vers orders"

faults:
  - service: payments
    type: downstream_slow
    target_endpoint: "POST /payments"
    params:
      latency_ms: 600
    duration_seconds: 90
    wait_before_seconds: 10
